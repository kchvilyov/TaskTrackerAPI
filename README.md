# TaskTracker API: Демонстрация перехода с Java/Kotlin на C# / .NET 10

Этот проект представляет собой простое Web API для управления задачами, созданное **за один день** в рамках подготовки к собеседованию на позицию C#/.NET-разработчика. Основная цель — продемонстрировать способность быстро осваивать новый технологический стек, опираясь на глубокий опыт в backend-разработке и принципиальную схожесть современных фреймворков.

## 🎯 Цель проекта

Практически исследовать ключевые концепции и инструменты экосистемы .NET 10, проводя параллели с уже известными технологиями (Java/Spring Boot и Kotlin/Quarkus), и создать работающий прототип как наглядное доказательство быстрой обучаемости.

## 🧠 Ключевые параллели: C#/.NET vs. Java/Kotlin

В ходе работы был проведён сравнительный анализ. Ниже — таблица основных концепций, показывающая, что переход заключается в изучении нового синтаксиса и инструментов для уже знакомых паттернов.

| Концепция / Задача | Реализация в C# / .NET 10 (данный проект) | Аналог в Java / Kotlin (мой опыт) | Комментарий |
| :--- | :--- | :--- | :--- |
| **Создание проекта** | `dotnet new webapi -n TaskTrackerAPI` | `start.spring.io` или шаблон в IDE | Идентичная идея: шаблонизированный старт. |
| **Управление зависимостями** | Файл `*.csproj` и менеджер пакетов NuGet (`dotnet add package`) | `pom.xml` / `build.gradle.kts` и Maven/Gradle | Разные инструменты, но те же принципы. |
| **Старт приложения** | Точка входа `Program.cs` с неявным `Main` | Класс с методом `public static void main(String[] args)` в Spring Boot | Отличие в синтаксисе, а не в архитектуре. |
| **Dependency Injection (DI)** | Встроенный контейнер. Регистрация: `builder.Services.AddScoped<IService, Service>()` | Контейнер Spring (`@Service`, `@Autowired`) или Quarkus (CDI) | **Один из главных параллелей.** Принципы (регистрация, внедрение через конструктор) идентичны. |
| **ORM и работа с БД** | Entity Framework Core (EF Core) с подходом Code First. Класс `DbContext`, `DbSet<T>`, LINQ-запросы. | JPA / Hibernate с `EntityManager`, `Repository`, Spring Data JPA. | Концепция единая: сущности, миграции, декларативные запросы. **LINQ в EF Core** — мощное расширение, аналогичное Stream API + QueryDSL. |
| **Создание REST API** | Контроллеры с атрибутами `[ApiController]`, `[HttpGet]`. Автоматическая сериализация в JSON. | Контроллеры Spring MVC с аннотациями `@RestController`, `@GetMapping`. | Практически полное соответствие модели MVC. |
| **Null-безопасность** | Ссылочные типы, допускающие null (`string?`), и операторы (`?.`, `??`). | Аннотации `@Nullable` в Java, встроенная null-безопасность типов (`String?`) в Kotlin. | **Идентичная философия** в Kotlin и современном C#. Позволяет писать более надежный код. |
| **Конфигурация** | Файл `appsettings.json`, доступ через `IConfiguration`. | Файлы `application.properties`/`yml` в Spring, доступ через `@Value`. | Единый подход к externalized configuration. |
| **Документация API** | Автогенерация Swagger UI через Swashbuckle (`Swagger.AspNetCore`). | Springdoc OpenAPI (`springdoc-openapi-starter-webmvc-ui`). | Одинаковый итог — интерактивная документация. |
| **Миграции БД** | Инструмент командной строки `dotnet ef migrations add` | Flyway, Liquibase или миграции Hibernate. | Автоматическая генерация на основе кода сущностей в EF Core. |
| **Асинхронность** | Ключевые слова `async`/`await`, тип `Task<T>`. | `CompletableFuture` в Java, корутины и `suspend`-функции в Kotlin. | Разные реализации, но общая цель — неблокирующая обработка запросов. Концептуально близко. |

## 🚀 Что реализовано в проекте

1.  **Базовая архитектура**: Чистое разделение на слои (Controllers, Models, Services, Data).
2.  **Модель данных**: Класс `TaskItem` (ID, Title, Description, Status, CreatedAt).
3.  **Доступ к данным**: Репозиторий на основе `DbContext` (Entity Framework Core) с использованием SQLite для простоты.
4.  **REST API**:
    *   `GET /api/tasks` — Получить список всех задач.
    *   `GET /api/tasks/{id}` — Получить задачу по ID.
    *   `POST /api/tasks` — Создать новую задачу.
    *   `PUT /api/tasks/{id}` — Обновить задачу.
    *   `DELETE /api/tasks/{id}` — Удалить задачу.
5.  **Документация**: Полностью настроен Swagger UI для тестирования и демонстрации эндпоинтов.
6.  **Контейнеризация (опционально)**: Добавлен `Dockerfile` для сборки контейнера приложения.

### Пример кода: Параллель в создании сервиса
**C# (.NET 10)**
```csharp
// Services/TaskService.cs
public class TaskService : ITaskService
{
    private readonly AppDbContext _context;
    // Внедрение зависимости через конструктор (как в Spring)
    public TaskService(AppDbContext context) => _context = context;

    public async Task<List<TaskItem>> GetAllAsync()
    {
        // Использование LINQ для декларативного запроса
        return await _context.Tasks
            .Where(t => t.IsCompleted == false)
            .OrderByDescending(t => t.CreatedAt)
            .ToListAsync();
    }
}
```

**Kotlin (Spring Boot)**
```kotlin
// TaskService.kt
@Service
class TaskService(@Autowired private val repository: TaskRepository) {
    suspend fun getAll(): List<TaskItem> {
        // Использование Kotlin coroutines и extension-функций
        return repository.findByIsCompletedFalse()
            .sortedByDescending { it.createdAt }
    }
}
```

## 💡 Выводы и приобретенные навыки

1.  **Быстрое освоение**: Основные архитектурные паттерны (MVC, DI, Repository) в .NET полностью соответствуют таковым в Spring/Quarkus, что позволило сосредоточиться на синтаксисе C# и особенностях инструментов (EF Core, NuGet).
2.  **Мощь LINQ**: Изучение Language Integrated Query (LINQ) показало его выразительность и удобство для работы с данными, что является приятным дополнением к опыту работы со Stream API и Spring Data JPA.
3.  **Эффективность инструментов**: Рабочий процесс с `dotnet CLI`, встроенным DI-контейнером и автогенерацией миграций EF Core в среде **.NET 10** оказался очень отлаженным и интуитивно понятным.
4.  **Готовность к работе**: В рамках одного дня удалось не только изучить основы, но и создать полностью рабочее, документированное и готовое к развертыванию API на актуальной платформе **.NET 10**, что подтверждает способность к быстрому вхождению в новый технологический стек для решения бизнес-задач.

Этот проект служит практическим доказательством того, что мой **20+-летний опыт в проектировании сложных backend-систем** является переносимым активом. Глубокое понимание основ позволяет эффективно осваивать новые инструменты, такие как C# 14 и .NET 10, применяя проверенные принципы чистой архитектуры, модульного тестирования и разработки через интерфейсы.

---
**Технологический стек проекта**: C# 14, .NET 10, ASP.NET Core, Entity Framework Core 10, SQLite, Swagger (Swashbuckle).